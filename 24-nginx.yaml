- name: configure nginx
  hosts: dynamic_frontend:dynamic_backend:dynamic_middle
  become: yes
  serial: 1
  tasks:

  - name: install
    ansible.builtin.dnf:
      name: nginx
      state: present

  - name: start and enable nginx
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes