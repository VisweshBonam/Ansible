- name: install packages through loops
  hosts: frontend
  become: yes
  tasks:
  - name: install packages
    ansible.builtin.packages:
      name: {{ item }}
      state: present
    
    loop:
    - nginx
    - zip
    - mysql
     